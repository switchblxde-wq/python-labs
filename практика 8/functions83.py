# Создаём функцию суммы двух чисел для параметризованных тестов.
def сумма(а, б):
    # Возвращаем сумму переданных чисел.
    return а + б

# Создаём функцию проверки чётности числа.
def чётное(число):
    # Возвращаем истину, если число делится на два без остатка.
    return число % 2 == 0
